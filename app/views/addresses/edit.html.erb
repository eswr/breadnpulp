<% provide(:title, "#{@address.name}") %>
<h1><%= "Edit my #{@address.name} address" %></h1>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= render 'shared/address_form' %>
	</div>
</div>