<% if @delivery.rider %>
	$('#rider_details_<%= @delivery.id %>').html("<p>Rider: <%= @delivery.rider.name %> | <%= @delivery.rider.phone_number %>");
<% else %>
	$('#rider_details_<%= @delivery.id %>').html("<p>Rider not allotted</p>")
<% end %>